	// Verifica se o x é menor que o y, e se o y é menor do que o z
	// Dá return a 1 se a condição for verdadeira e 0 se for falsa;
	
	
int check ( int x, int y, int z){
	
	if ( (x < y) && ( y < z ) ) return 1 ;
	else return 0;
	
	
	
}
